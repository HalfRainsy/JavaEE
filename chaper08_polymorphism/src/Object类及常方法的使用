1：object类的说明：
    >明确：java.lang.Object
    >任何一个Java类(除Object)都直接或间接的结成与Object类。
    >Object类称为Java类的根父类
    >Object声明的结构(属性、方法)具备通用性。
        >object没有属性
        >object提供了一个空参的构造器
        >重点关注：object类中声明的方法


2：常用方法
    重点方法：equals()/toString()
    了解方法：clone() /finalize()
    目前不需要关注：